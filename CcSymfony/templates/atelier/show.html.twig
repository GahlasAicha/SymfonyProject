{% extends 'base.html.twig' %}

{% block title %}Atelier{% endblock %}

{% block body %}
    <h1>Atelier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ atelier.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ atelier.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>
                    {# Afficher la description en HTML après conversion Markdown #}
                    <div>{{ atelier.descriptionHtml|raw }}</div>
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_atelier_index') }}">Ratour a la liste </a>

    <a href="{{ path('app_atelier_edit', {'id': atelier.id}) }}">edit</a>

    {{ include('atelier/_delete_form.html.twig') }}
{% endblock %}
